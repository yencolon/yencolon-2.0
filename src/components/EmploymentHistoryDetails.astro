---
import Anchor from "./common/Anchor.astro";
import type { EmploymentHistoryFrontmatter } from "../lib/types";

type Props = EmploymentHistoryFrontmatter;

const {
  title,
  company,
  description,
  startDate,
  endDate,
  city,
  country,
  stack,
  bullets,
} = Astro.props;

console.log(stack);
---

<div class="flex flex-col gap-3 pb-8">
  <div class="flex flex-col gap-3 sm:flex-row sm:justify-between">
    <Anchor url={"/"} class="text-xl">
      {title}
    </Anchor>
    <div class="flex gap-2">
      <Anchor url={company} class="text-sm">{company}</Anchor>
    </div>
  </div>
  <p class="zag-text zag-transition">
    {description}
  </p>

  <p class="zag-text zag-transition">
    {startDate} - {endDate}
  </p>
  <p class="zag-text zag-transition">
    {city}, {country}
  </p>
  <p class="zag-text zag-transition">
    {
      stack.map((tech) => (
        <span class="text-sm bg-gray-100 px-2 py-1 rounded-full">{tech}</span>
      ))
    }
  </p>

  <ul class="list-disc list-inside zag-text zag-transition">
    {bullets.map((bullet) => <li>{bullet}</li>)}
  </ul>
</div>
